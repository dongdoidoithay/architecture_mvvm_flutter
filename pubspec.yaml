name: flutter_architecture_mvvm
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.17.6 <3.0.0"

dependencies:
  flutter:
    sdk: flutter


  cupertino_icons: ^1.0.2
  flutter_localization: ^0.1.10
  # For Architecture
  flutter_riverpod: ^2.3.1
  # For Networking
  dio: ^5.0.2
  # For Model
  json_serializable: ^3.5.0
  freezed_annotation: ^0.12.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0
  # For Model
  freezed: ^0.12.6
  build_runner: ^1.10.12


flutter:
  uses-material-design: true
  generate: true

